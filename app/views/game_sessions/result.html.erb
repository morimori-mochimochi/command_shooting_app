<h1>
  只今の
<%= user_signed_in? ? "#{current_user.nickname}さん" : "あなた" %>
  の結果は！！！
</h1>
<div class="result-container">
  <%= @game_session.duration %> 秒
  <p>ログインしてランキングに参加しよう！</p>
  <% unless user_signed_in? %>
    <%= button_to 'ログイン', new_user_session_path, method: :get, data: { turbo: false } %>
  <% end %>
  <%= button_to 'ランキング', rankings_path, method: :get, data: { turbo: false } %>
  <%= button_to 'もう一度', standby_path, method: :get, data: { turbo: false } %>
  <%= button_to 'トップへ', root_path, method: :get, data: { turbo: false } %>
</div>

         